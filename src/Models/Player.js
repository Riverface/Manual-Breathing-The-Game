import "./Resource.js";

import { types } from "@babel/core";



export default function Player(firstName, lastName, date, time, resources) {

    (this.name = { first: firstName, last: lastName }),
    this.Date = date,
        this.Time = time;


}